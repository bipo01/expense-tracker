<style>
  .addExpenseContainer {
    position: fixed;
    top: 0;
    left: 0;
    background-color: rgba(0, 0, 0, 0.853);
    width: 100%;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 999;
    padding: 20px;
    box-sizing: border-box;
  }

  #addExpenseForm {
    display: flex;
    flex-direction: column;
    gap: 20px;
    width: 100%;
    max-width: 600px;
    background-color: rgb(27, 27, 27);
    border: 1px solid rgb(56, 56, 56);
    border-radius: 16px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.4);
    padding: 30px;
    box-sizing: border-box;
    color: white;
  }

  .expense-category-div,
  .local-date-div,
  .submit-cancel {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  #addExpenseForm input,
  #addExpenseForm select,
  #addExpenseForm button {
    padding: 12px 16px;
    border-radius: 8px;
    border: 1px solid rgb(56, 56, 56);
    background: rgb(20, 20, 20);
    color: white;
    outline: none;
    flex: 1;
    min-width: 100px;
  }

  #addExpenseBtn {
    background-color: rgb(235, 130, 130);
    color: black;
    font-weight: bold;
  }

  #cancelBtnExpense {
    background-color: rgb(63, 63, 63);
    color: white;
  }

  #addExpenseBtn:hover,
  #cancelBtnExpense:hover {
    filter: brightness(110%);
    cursor: pointer;
  }

  #addExpenseBtn {
    width: 100%;
    background-color: rgb(125, 26, 26) !important; /* tom mais forte e vivo */
    color: white;
    font-weight: bold;
    border: none;
    border-radius: 8px;
    padding: 12px 16px;
    transition: background-color 0.2s ease, transform 0.1s ease;
  }

  #addExpenseBtn:hover {
    background-color: rgb(255, 110, 110); /* tom mais claro no hover */
    cursor: pointer;
    transform: scale(1.02);
    filter: brightness(110%);
  }

  /* Responsividade para telas menores */
  @media (max-width: 600px) {
    #addExpenseForm {
      padding: 20px;
    }

    .expense-category-div,
    .local-date-div,
    .submit-cancel {
      flex-direction: column;
    }

    #addExpenseForm input,
    #addExpenseForm select,
    #addExpenseForm button {
      width: 100%;
    }

    #addExpenseBtn,
    #cancelBtnExpense {
      width: 100%;
    }
  }
</style>
