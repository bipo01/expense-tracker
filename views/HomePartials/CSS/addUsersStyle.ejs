<style>
  #addUsersContainer {
    position: fixed;
    top: 0;
    right: 0;
    height: 100vh;
    width: 100vw;
    background: rgba(0, 0, 0, 0.85);
    z-index: 999;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .addUsersInnerContainer {
    position: relative; /* necessário para posicionar o ícone internamente */
    background: rgb(15, 15, 15);
    width: 40%;
    height: 80%;
    border-radius: 10px;
    padding: 40px;
    color: white;
    display: flex;
    flex-direction: column;
    gap: 15px;
    overflow-y: auto;
    border: 1px solid rgb(56, 56, 56);
    align-items: center;
    text-align: center;
  }

  .addUsersInnerContainer img {
    margin-top: 20px;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 255, 0, 0.1);
  }

  .addUsersInnerContainer p {
    font-size: 16px;
    color: rgba(200, 200, 200, 0.9);
    margin-top: 20px;
    max-width: 80%;
    line-height: 1.4;
  }

  .addUsersInnerContainer > i#closeAddUsersTab {
    position: absolute;
    top: 10px;
    right: 10px;
    font-size: 24px;
    color: rgba(255, 255, 255, 0.7);
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .addUsersInnerContainer > i#closeAddUsersTab:hover {
    color: rgba(255, 100, 100, 1);
    transform: scale(1.2);
  }

  #searchUserToAdd {
    padding: 10px 15px;
    border-radius: 8px;
    width: 100%;
    border: none;
    outline: none;
    background: rgb(30, 30, 30);
    color: white;
    font-size: 15px;
    border: 1px solid rgb(56, 56, 56);
  }

  .addUserDiv {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgb(27, 27, 27);
    padding: 12px 15px;
    border-radius: 8px;
    border: 1px solid rgb(56, 56, 56);
    transition: background 0.3s ease;
  }

  .addUserDiv:hover {
    background: rgb(35, 35, 35);
  }

  .addUserDiv span {
    color: white;
    font-weight: 500;
    font-size: 15px;
    margin-right: 10px;
  }

  .addUserDiv span:last-of-type {
    color: rgba(200, 200, 200, 0.7);
    font-size: 14px;
  }

  .addUser {
    background: rgba(50, 255, 50, 0.2);
    border: none;
    border-radius: 6px;
    padding: 8px 10px;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .addUser i {
    color: rgb(0, 255, 0);
    font-size: 18px;
  }

  .addUser:hover {
    background: rgba(50, 255, 50, 0.35);
    transform: scale(1.1);
  }

  .search-bar-wrapper {
    position: relative;
    width: 100%;
  }

  .search-bar-wrapper input {
    padding: 10px 45px 10px 15px;
    border-radius: 8px;
    width: 100%;
    border: 1px solid rgb(56, 56, 56);
    background: rgb(30, 30, 30);
    color: white;
    font-size: 15px;
    outline: none;
  }

  #searchNewUser {
    position: absolute;
    right: 10px;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    color: rgba(255, 255, 255, 0.6);
    font-size: 18px;
    cursor: pointer;
    padding: 5px;
    transition: color 0.2s ease, transform 0.2s ease;
  }

  #searchNewUser:hover {
    color: rgb(130, 235, 130);
  }

  .usersSearched {
    width: 100%;
    display: flex;
    align-items: center;
    flex-direction: column;
    gap: 10px;
    margin-top: 15px;
  }

  .user {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgb(21, 21, 21);
    padding: 12px 15px;
    border-radius: 8px;
    border: 1px solid rgb(56, 56, 56);
    transition: background 0.3s ease;
  }

  .user:hover {
    background: rgb(35, 35, 35);
  }

  .user p {
    margin: 0;
    color: white;
    font-weight: 500;
    font-size: 15px;
  }

  #usernameUserSearched {
    color: rgba(200, 200, 200, 0.7);
    font-size: 14px;
  }

  .user i {
    color: rgb(0, 255, 0);
    font-size: 18px;
    cursor: pointer;
    transition: transform 0.2s ease;
  }

  .user i:hover {
    transform: scale(1.2);
  }

  .dot-loader {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 8px;
    height: 50px;
    margin-top: 20px;
  }

  .dot-loader span {
    width: 10px;
    height: 10px;
    background: rgb(
      130,
      235,
      130
    ); /* verde claro igual aos ícones de entrada */
    border-radius: 50%;
    animation: bounce 1.2s infinite;
  }

  .dot-loader span:nth-child(2) {
    animation-delay: 0.2s;
  }

  .dot-loader span:nth-child(3) {
    animation-delay: 0.4s;
  }

  @keyframes bounce {
    0%,
    80%,
    100% {
      transform: translateY(0);
      opacity: 0.5;
    }
    40% {
      transform: translateY(-12px);
      opacity: 1;
    }
  }

  @media (max-width: 650px) {
    .addUsersInnerContainer {
      width: 90%;
      height: 90%;
      padding: 20px;
      border-radius: 12px;
    }

    .addUsersInnerContainer p {
      font-size: 14px;
      max-width: 100%;
    }

    .addUserDiv,
    .user {
      flex-direction: column;
      align-items: flex-start;
      gap: 10px;
      padding: 12px;
    }

    .addUser {
      align-self: flex-end;
    }

    .search-bar-wrapper input {
      font-size: 14px;
      padding: 10px 40px 10px 12px;
    }

    #searchNewUser {
      font-size: 16px;
      right: 8px;
    }

    .addUsersInnerContainer > i#closeAddUsersTab {
      font-size: 22px;
      top: 12px;
      right: 12px;
      z-index: 99999;
    }
  }
</style>
