<style>
  .addEntryContainer {
    position: fixed;
    top: 0;
    left: 0;
    background-color: rgba(0, 0, 0, 0.853);
    width: 100%;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 999;
    padding: 20px;
    box-sizing: border-box;
  }

  #addEntryForm {
    display: flex;
    flex-direction: column;
    gap: 20px;
    width: 100%;
    max-width: 600px;
    background-color: rgb(27, 27, 27);
    border: 1px solid rgb(56, 56, 56);
    border-radius: 16px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.4);
    padding: 30px;
    box-sizing: border-box;
    color: white;
  }

  .entry-category-div,
  .local-date-div,
  .submit-cancel {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  #addEntryForm input,
  #addEntryForm select,
  #addEntryForm button {
    padding: 12px 16px;
    border-radius: 8px;
    border: 1px solid rgb(56, 56, 56);
    background: rgb(20, 20, 20);
    color: white;
    outline: none;
    flex: 1;
    min-width: 100px;
  }

  #addEntryBtn {
    background-color: rgb(130, 235, 130);
    color: black;
    font-weight: bold;
  }

  #cancelBtnEntry {
    background-color: rgb(63, 63, 63);
    color: white;
  }

  #addEntryBtn:hover,
  #cancelBtnEntry:hover {
    filter: brightness(110%);
    cursor: pointer;
  }

  #addEntryBtn {
    width: 100%;
    background-color: rgba(
      16,
      96,
      16,
      0.73
    ) !important; /* tom mais forte e vivo */
    color: white;
    font-weight: bold;
    border: none;
    border-radius: 8px;
    padding: 12px 16px;
    transition: background-color 0.2s ease, transform 0.1s ease;
  }

  #addEntryBtn:hover {
    background-color: rgb(51, 121, 51); /* tom mais claro no hover */
    cursor: pointer;
    transform: scale(1.02);
    filter: brightness(110%);
  }

  /* Responsividade para telas menores */
  @media (max-width: 600px) {
    #addEntryForm {
      padding: 20px;
    }

    .entry-category-div,
    .local-date-div,
    .submit-cancel {
      flex-direction: column;
    }

    #addEntryForm input,
    #addEntryForm select,
    #addEntryForm button {
      width: 100%;
    }

    #addEntryBtn,
    #cancelBtnEntry {
      width: 100%;
    }
  }
</style>
